# Evaluation Workspace Rules

- You may modify files only under `src/`.
- Do not modify `package.json`, `tsconfig.json`, this `AGENTS.md`, or anything under `tests/`.
- Do not add dependencies, generated files, or replacement test suites.
- Preserve behavior not explicitly requested by the task.
- Run `pnpm typecheck` and `pnpm test` before finishing.
