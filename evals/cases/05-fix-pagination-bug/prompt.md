用户列表支持按页码查询，也保留了直接传入 offset 的旧调用方式。当前按页码查询时会跳过不该跳过的记录，请定位并修复分页边界问题。

要求：
- 页码从 1 开始，pageSize 表示每页数量。
- 显式传入 offset 时继续按原有方式工作，并优先于 page。
- 保留默认参数、结果顺序以及非法参数校验行为。
- 只允许修改 `src/` 下的文件。
- 完成后运行项目已有的类型检查和测试。
