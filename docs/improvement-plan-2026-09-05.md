# 2026-09-05 审查问题改进计划

## 范围与依据

当前基线：`fab8f9c`，工作区干净。本次处理审查列出的 12 项行为问题及直接相关的调用链；依据提交说明、README、现有行为契约和已安装依赖的接口实现。

主要边界为权限策略、SSE 解析、TUI 静态历史、批量工具输出、Fork 调度、MCP 连接、Hook 生命周期、技能下载、工作树回收和摘要恢复。保留 OpenAI 缺少 `finish_reason` 时失败、指令文件支持 home/绝对路径等已有契约。

## 修改清单与验收条件

| 序号 | 问题                              | 改进方法                                                         | 验收条件                                                             |
| ---- | --------------------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------- |
| 1    | 严格只读模式被允许规则绕过        | 将只读硬约束放在可覆盖的允许规则之前，并检查实际工具执行入口     | 已有任务/持久允许规则也不能放行写入；真正的只读操作仍可执行          |
| 2    | MCP 参数中的 URL 导致通配拒绝失效 | 区分参数字符串与文件路径的通配语义，保留精确参数授权             | 拒绝规则覆盖 URL、反斜杠和嵌套参数；精确允许不扩展到其他参数         |
| 3    | sudo/doas 包装后的危险命令漏检    | 正确处理包装命令的选项和值，再检查实际命令                       | 带用户、组等选项的危险调用仍被硬拒绝                                 |
| 4    | 损坏 SSE 事件被静默丢弃           | 对非空、非结束标记的数据解析失败明确报错                         | 不完整工具参数不能形成成功响应或进入执行；合法心跳和结束标记保持兼容 |
| 5    | TUI 会话切换复用旧静态边界        | 随静态历史世代重置状态，并处理历史删除对边界的影响               | 从长会话切到短会话后，新回复保持流式更新，不重复输出已提交历史       |
| 6    | 工作区回执污染批量步骤引用        | 保留原始步骤输出，将回执放在面向模型的结果中                     | 后续 `$ref`/字符串插值收到原始 JSON/字符串，同时模型能看到工作区变化 |
| 7    | Fork 并发统计包含排队任务         | 区分运行与排队任务，统一调度和取消/超时结算                      | 并发上限 1、4 均准确；排队任务取消或超时后不会留下未决等待           |
| 8    | MCP 覆盖 SDK 断连回调             | 使用客户端生命周期回调，保留 SDK 清理路径                        | 断连后在途调用及时失败，失效工具下线，关闭流程可完成                 |
| 9    | 异步 once hook 并发重复执行       | 对执行中的 once 动作占位，失败释放，成功消费                     | 并发事件只执行一次；失败允许重试；会话 reset 不受旧动作完成影响      |
| 10   | 多跳相对重定向基址错误            | 每跳更新当前 URL，并继续执行协议和跳数限制                       | 跨目录/域名后的相对跳转正确，HTTPS 降级仍拒绝                        |
| 11   | 孤儿工作树始终无法回收            | 使用可验证的提交关系判断是否可回收，保留有改动或独有提交的工作树 | 干净且可安全回收的孤儿被清理；受保护、未提交改动或未合并提交保留     |
| 12   | 非英文摘要依赖 analysis 标签      | 将草稿剥离与结构判断分开，允许没有草稿标签的结构化摘要           | 中文等语言的编号摘要可恢复；空摘要、仅草稿或无结构内容仍拒绝         |

## 执行顺序

- [x] 复核当前工作区、代码边界、构建脚本及 CI 工作流。
- [x] 完成改进计划。
- [ ] 完成 12 项代码修复，并静态检查相关调用链。
- [ ] 集中执行类型检查与生产构建，修复发现的问题。
- [ ] 全局安装构建后的本地 CLI，验证命令入口、版本和帮助输出。
- [ ] 按 `.github/workflows/ci.yml` 在本地验证全部检查项。
- [ ] 清理临时回归用例和临时验证产物，复核最终差异。
- [ ] 提交修复并推送 `origin/main`，核对远程提交及 CI 状态。

## 验证安排

修改过程中不逐次运行测试。代码完成后先执行 `pnpm typecheck`、`pnpm build`，成功后安装全局 CLI；再依次执行 CI 命令：

1. `pnpm install --frozen-lockfile`
2. `pnpm format:check`
3. `pnpm lint`
4. `pnpm typecheck`
5. `pnpm build`
6. `pnpm coverage`

CI 使用 Node.js 22、pnpm 10.30.3 和 Ubuntu。本地验证尽量匹配 Node.js 22 与 pnpm 版本，并明确记录 Windows 与 Ubuntu 的平台差异；推送后核对实际远程 CI。

必要的定向回归验证集中安排在最后的验证阶段，覆盖权限边界、工具参数完整性、并发与生命周期；临时用例验证通过后清理，保留仓库原有测试。若本地 CI 暴露已有但阻碍交付的问题，在直接相关的最小范围内修复，重新验证受影响步骤；代码变化后重新构建并更新全局安装。

## 执行记录

- 初始状态：本地 `main` 与 `origin/main` 同为 `fab8f9c`，无未提交修改。
- 本机初始工具链：Node.js 24.11.0、pnpm 10.30.3、npm 11.6.1。
- 全局 npm 前缀：`D:/Program Files/nodejs/node_global`；已有 `oran` 命令入口。
- 计划以用户授权的完整链路为完成条件；任何未完成的安装、检查或推送步骤均不视为交付完成。
